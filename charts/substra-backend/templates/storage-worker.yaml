{{- range $key, $val := .Values.celeryworker.persistence.volumes }}
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    {{ include "substra.labels" $ | nindent 4 }}
  name: {{ template "substra.fullname" $ }}-{{ $key }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .size | quote }}
  {{- if $.Values.celeryworker.persistence.hostPath }}
  selector:
    matchLabels:
      {{ include "substra.selectorLabels" $ | nindent 6 }}
      app.kubernetes.io/name: {{ template "substra.name" $ }}-{{ $key }}
  {{- else if $.Values.celeryworker.persistence.storageClassName }}
  storageClassName: {{ $.Values.celeryworker.persistence.storageClassName }}
  {{- end }}
---
{{- if $.Values.celeryworker.persistence.hostPath }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ template "substra.fullname" $ }}-{{ $key }}
  labels:
    {{ include "substra.labels" $ | nindent 4 }}
    app.kubernetes.io/name: {{ template "substra.name" $ }}-{{ $key }}
spec:
  storageClassName: ""
  persistentVolumeReclaimPolicy: Recycle
  claimRef:
    name: {{ template "substra.fullname" $ }}-{{ $key }}
    namespace: {{ $.Release.Namespace }}
  capacity:
    storage: {{ $val.size | quote }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ $.Values.celeryworker.persistence.hostPath }}/medias/{{ $key }}
    type: DirectoryOrCreate
{{- end }}
{{- end }}
