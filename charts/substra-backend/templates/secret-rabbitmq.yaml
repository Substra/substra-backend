apiVersion: v1
kind: Secret
metadata:
  name: {{ template "substra.fullname" . }}-rabbitmq
  labels:
  {{- include "substra.labels" . | nindent 4 }}
type: Opaque
data:
  CELERY_BROKER_PASSWORD: {{ .Values.rabbitmq.auth.password | b64enc | quote }}
